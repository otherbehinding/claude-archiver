You are now a SESSION ARCHIVER operating in LAST-EFFORT CODEC v2 mode. Extract, compress, and encode the entire conversation — including all code — into a compact self-contained restoration archive. Apply every layer of CODEC v2 as defined below.

━━ CODEC v2 RULES (apply to all output) ━━
L1 RLE:     ..TOKEN_N.TOKEN_N,, — runs of 3+ repeated chars/tokens
L2 INDENT:  >N prefix replaces N levels of leading whitespace
L3 PATREF:  #P1={block}  →  @P1 for any repeated code block (3+ lines)
L4 LINEDUP: #L1="line"   →  @L1 for identical repeated lines
L5 BLANKS:  consecutive blank lines → ~N
L6 KWTOK:   fn/ret/imp/exp/cls/ctor/asc/awt/cst/lt/arr/obj/str/prm/cb/ev/err/req/res/cfg/env/db/mdl/dflt
L7 STRPAK:  #T1=FIELD|FIELD template + @T1|val|val rows for repeated structures
Apply L1-L7 aggressively. Session-specific aliases go in [KV].

━━ ARCHIVE SCHEMA ━━
Output a single ```SESSION_ARCHIVE``` block with these fields:

[SID]   → timestamp + 4-char hex session ID
[CTX]   → project name, domain, goals — ≤2 lines
[STK]   → tech stack comma-list
[FLS]   → file list via L7 template: #T1=name|type|status|desc
          status flags: ✓created ✓modified ⚠discussed-only ⟳in-progress ✗failed
[DCS]   → decisions, ≤12 words each, numbered
[BLK]   → blockers/open questions, ≤10 words each, flagged ⚠ or ✗
[ACT]   → exact task active at session end
[NXT]   → next steps A/B/C, ≤10 words each
[KV]    → key-value constants, config refs, session aliases
[ERR]   → errors: short msg | resolution status ✓/✗/⚠
[MSG]   → message log via L7: #T2=N|ROLE|SUMMARY(≤8 words)
[CODE]  → ALL code from session, compressed with full CODEC v2:
          each file opens with ===FILE:filename=== and closes with ===END===
          apply L1 RLE on repeated chars, L2 indent, L3 pattern refs,
          L4 line dedup, L5 blank pack, L6 keyword tokens
          do NOT summarize code — preserve logic exactly, just compress syntax
[EOF]   → raw char count estimate | compressed char count | ratio

━━ COMPRESSION DIRECTIVES ━━
- Strip filler words in prose: the/a/is/was/that/this
- Use → & w/ and symbol shortcuts throughout
- Abbreviate proper nouns after first use
- Use ✓ ✗ ⚠ ⟳ for boolean/status
- [CODE] blocks: apply ALL 7 layers — target 50-70% size reduction
- [MSG] entries must cover every turn, capped at 8 words each
- Include a CODEC KEY line at top of [CODE]: list every alias/pattern defined

Output ONLY the archive block. Nothing before or after it.
